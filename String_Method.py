# Here's a concise table with **all 47 Python string methods** along with **a short description**:  

# | **Method**       | **Description** |
# |-----------------|----------------|
# | `upper()`       | Converts all characters to uppercase |
# | `lower()`       | Converts all characters to lowercase |
# | `title()`       | Capitalizes the first letter of each word |
# | `capitalize()`  | Capitalizes the first letter of the string |
# | `swapcase()`    | Swaps uppercase to lowercase and vice versa |
# | `isalpha()`     | Returns `True` if all characters are alphabets |
# | `isdigit()`     | Returns `True` if all characters are digits |
# | `isalnum()`     | Returns `True` if all characters are alphanumeric |
# | `isspace()`     | Returns `True` if all characters are spaces |
# | `isdecimal()`   | Returns `True` if all characters are decimal digits |
# | `isnumeric()`   | Returns `True` if all characters are numeric |
# | `isidentifier()` | Returns `True` if string is a valid Python identifier |
# | `islower()`     | Returns `True` if all characters are lowercase |
# | `isupper()`     | Returns `True` if all characters are uppercase |
# | `istitle()`     | Returns `True` if string is in title case |
# | `isprintable()` | Returns `True` if all characters are printable |
# | `find(sub)`     | Returns the first index of `sub`, or `-1` if not found |
# | `rfind(sub)`    | Returns the last index of `sub`, or `-1` if not found |
# | `index(sub)`    | Returns the first index of `sub`, raises an error if not found |
# | `rindex(sub)`   | Returns the last index of `sub`, raises an error if not found |
# | `count(sub)`    | Returns the number of times `sub` appears in the string |
# | `replace(old, new)` | Replaces all occurrences of `old` with `new` |
# | `format()`      | Formats strings using placeholders `{}` |
# | `format_map()`  | Similar to `format()`, but uses a dictionary |
# | `maketrans()`   | Creates a translation table for string replacement |
# | `translate(table)` | Replaces characters based on a translation table |
# | `split(sep)`    | Splits the string into a list using `sep` as a delimiter |
# | `rsplit(sep)`   | Splits the string from the right side |
# | `splitlines()`  | Splits string at line breaks (`\n`) |
# | `partition(sep)` | Splits into three parts: before `sep`, `sep`, after `sep` |
# | `rpartition(sep)` | Similar to `partition()`, but splits from the right |
# | `join(iterable)` | Joins elements of an iterable with the string as separator |
# | `strip()`       | Removes spaces from both ends |
# | `lstrip()`      | Removes spaces from the left side |
# | `rstrip()`      | Removes spaces from the right side |
# | `startswith(sub)` | Returns `True` if string starts with `sub` |
# | `endswith(sub)` | Returns `True` if string ends with `sub` |
# | `center(width)` | Centers the string within `width` spaces |
# | `ljust(width)`  | Left-aligns the string within `width` spaces |
# | `rjust(width)`  | Right-aligns the string within `width` spaces |
# | `zfill(width)`  | Pads the string with zeros on the left |
# | `encode()`      | Encodes the string into bytes |
# | `decode()`      | Decodes bytes into a string |
# | `casefold()`    | Converts to lowercase (stronger than `lower()`) |
# | `expandtabs(n)` | Replaces tabs (`\t`) with `n` spaces |

# ### **Total Methods: 47 ✅**  

# Let me know if you need more details! 🚀